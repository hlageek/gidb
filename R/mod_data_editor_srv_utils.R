default_game_data <- function() {
  list(
    info = list(
      title = NULL,
      release_year = NULL,
      description = NULL,
      official_url = NULL
    ),
    identifiers = list(
      moby_id = NULL
    ),
    game_tags = list(
      list(name = NULL, category = NULL)
    ),
    platforms = list(
      list(name = NULL, year = NULL)
    ),
    originators = list(
      list(
        name = NULL,
        role = NULL,
        location = NULL
      )
    ),
    notes = NULL,
    mobygames_called = NULL
  )
}
